import { CheckActivePipe } from './check-active.pipe';

describe('CheckActivePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckActivePipe();
    expect(pipe).toBeTruthy();
  });
});
