import { FormatInstanceofDatePipe } from './format-instanceof-date.pipe';

describe('FormatInstanceofDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInstanceofDatePipe();
    expect(pipe).toBeTruthy();
  });
});
